import { CccardhoverDirective } from './cccardhover.directive';

describe('CccardhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CccardhoverDirective();
    expect(directive).toBeTruthy();
  });
});
